PORT=4000
TOP_N_COINS=20
# Lista de orígenes permitidos por CORS separados por coma. Vacío = permitir todos (comportamiento por defecto).
# Ejemplo: CORS_ORIGINS=http://localhost:5173,https://tradia.example.com
# CORS_ORIGINS=
