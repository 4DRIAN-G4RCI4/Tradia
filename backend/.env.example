PORT=4000
TOP_N_COINS=20
# API key gratuita (plan Demo) de CoinGecko: https://www.coingecko.com/en/developers/dashboard
# Vacío = peticiones anónimas (cuota compartida, se agota rápido en producción).
COINGECKO_API_KEY=
# Lista de orígenes permitidos por CORS separados por coma. Vacío = permitir todos (comportamiento por defecto).
# Ejemplo: CORS_ORIGINS=http://localhost:5173,https://tradia.example.com
# CORS_ORIGINS=
